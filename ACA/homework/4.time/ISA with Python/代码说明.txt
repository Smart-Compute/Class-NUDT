代码使用了两个循环，第一个循环.tag进行赋值，第二个循环.loop对每个值进行更新，即每个元素都＋5。

第二个循环中使用了Loop UNrolling技术，在一个循环内部执行五个单个循环语句，这样原先的1000次循环，就能够在200次循环后结束。这样可以减少流水线冒险，减少气泡，也就减少了CPI，加快了程序执行效率。

由于自己写的解释器的问题，汇编代码中不能插入空格，请见谅。
程序从索引1001开始，倒序执行到索引1。
